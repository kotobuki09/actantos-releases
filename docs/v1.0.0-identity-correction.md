# v1.0.0 public identity correction (2026-07-11)

## Problem

The original public `v1.0.0` tag pointed at source whose `package.json` and local
manifest identified `0.1.0`. The separately uploaded `actantosd-1.0.0.tgz` asset
identified `1.0.0` but omitted the lockfile required by `npm ci` / `smoke:fresh-install`.

## History policy

Correct the public `v1.0.0` identity in place (retag source + replace release assets).
This is a pre-living-partner remediation; do not treat the superseded assets as the
installable Quiet Open-Core surface.

## Corrected identity

- Git tag: `v1.0.0`
- Package version: `1.0.0`
- Stage: `quiet-open-core`
- npm pack includes `npm-shrinkwrap.json` so `npm ci` works from the tarball
- Source tree includes `package-lock.json` so git-clone install and Docker builds work

## Verification

Two clean public-artifact installs are required under FWD-004 before living Pilot #1 acceptance.
