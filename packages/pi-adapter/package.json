{
  "name": "pi-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ActantOS Pi adapter package",
  "scripts": {
    "test": "node --experimental-strip-types --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2"
  }
}
